sudo apt update && sudo apt upgrade
lsmod | grep vboxguest
sudo apt-get upgrade
sudo apt-get install
sudo apt-get install gcc make perl
clear
sudo apt install bzip2
lsmod | grep vboxguest
ls /media
ls /media/sf_ARCHIVOS COMPARTIDOS CON LINUX VIRTUAL BOX
ls /media/sf_ARCHIVOS_COMPARTIDOS_CON_LINUX_VIRTUAL_BOX
sudo usermod -aG vboxsf $USER
sudo reboot
ls /media
ls /media/sfARCHIVOS_COMPARTIDOS_CON_LINUX_ VIRTUAL_BOX
ls /media/sf_ARCHIVOS_COMPARTIDOS_CON_LINUX_VIRTUAL_BOX
ln -s /media/sf_ARCHIVOS_COMPARTIDOS_CON_LINUX_VIRTUAL_BOX ~/Escritorio/MiCarpetaCompartida
sudo usermod -aG vboxsf $USER
ssh-keygen
cat $HOME/.ssh/*.pub
cd
mkdir repogit
cd repogit
git clone git@github.com:MiriamCoanque/TP_AySO.git
sudo apt install git
git clone git@github.com:MiriamCoanque/TP_AySO.git
cd TP_AySO/
pwd
ls -l
cat /etc/os-release
man grep
grep HOME_URL /etc/os-release
grep home_url /etc/os-release
grep -i home_url /etc/os-release
whoami
echo “Usuario=$(whoami)” > datos_usuario.txt
ls
git add.
git add .
git config --global user.email "nekonyaaann@gmail.com"
git config --global user.name "MiriamCoanque"
git commit -m "ADD: agregado 1er
ejercicio sobre datos_usuarios.txt"
git status
git push
grep "model name" /proc/cpuinfo | head -n1 >> datos_usuarios.txt
cat <<EOF >> README.md
Alumno: Miriam Coanque
División: 315
Turno: Noche
EOF

git add README.md datos_usuarios.txt
git commit -m "ADD: Añadiendo Readme y datos de usuario"
git push
history -a
